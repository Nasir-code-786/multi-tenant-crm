# Local development — copy to .env.local (never commit .env.local)
API_URL=http://localhost:3001/api

# --- Vercel production (set in Vercel dashboard, then redeploy) ---
# API_URL=https://your-api.up.railway.app/api
# Must include /api at the end. Redeploy required after changing.
